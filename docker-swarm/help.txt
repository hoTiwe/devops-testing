docker service ls
docker service ps postgres
docker service ps redis

# деплоим стек 
docker stack deploy -c docker-compose.yml mystack

docker ps --filter label=com.docker.swarm.service.name=mystack_ubuntu-client

apt-get update
apt-get install -y postgresql-client redis-tools ca-certificates iputils-ping netcat

psql -h postgres -U postgres -d postgres -c "SELECT 1;"

redis-cli -h redis -p 6379 PING

| Ситуация           | Команда                         |
| ------------------ | ------------------------------- |
| Деплой             | `docker stack deploy`           |
| Не стартует сервис | `docker service ps --no-trunc`  |
| Масштабирование    | `docker service scale`          |
| Обновление         | `docker service update --force` |
| Откат              | `docker service rollback`       |
| Логи               | `docker service logs -f`        |
| Где запущено       | `docker service ps`             |
